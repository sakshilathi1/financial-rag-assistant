# OpenAI API Key (optional — falls back to template-based generation without it)
OPENAI_API_KEY=sk-...

# SEC EDGAR User-Agent (required by SEC — use your real email)
SEC_USER_AGENT="Financial RAG Assistant contact@example.com"
